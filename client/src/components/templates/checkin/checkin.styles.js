import styled from 'styled-components'

export const Main = styled.main`
    height: calc(100vh);
    width: 100vw;
    div.stepsWrapper {
        max-height: 100%;
        display: flex;
        flex-direction: row;
        width: 100vw;
        /* overflow: hidden; */
        transition: 0.2s ease;
        /* overflow-y: hidden; */
        /* overflow-x: scroll; */
        /* overflow-x: scroll; */
    }
`
